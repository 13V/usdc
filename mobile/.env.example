# The Divvy API server the app talks to. On a phone, "localhost" is the phone
# itself — point this at your computer's LAN IP (find it with `ipconfig` /
# `ifconfig`) or a deployed server. Must be reachable from the device.
EXPO_PUBLIC_API_URL=http://192.168.1.50:3000
